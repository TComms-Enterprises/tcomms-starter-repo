stages: [lint, test]
image: python:3.11
before_script:
  - pip install -r requirements.txt -r requirements-dev.txt
lint:
  stage: lint
  script: ["bandit -r src -ll"]
test:
  stage: test
  script: ["pytest -q"]
